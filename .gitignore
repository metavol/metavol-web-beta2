.DS_Store
node_modules
/dist

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings
.claude/

# Local DICOM sample data (served via /api/cases for dev only)
# 中身 (症例フォルダ・DICOM) は無視。README だけ同梱ガイドとして tracked。
# 注: 'sample-data/' だと親ディレクトリごと除外され !README の再包含が効かないため
#     'sample-data/*' で「中身のみ無視」にする必要がある。
sample-data/*
!sample-data/README.md

# Playwright MCP scratch output
.playwright-mcp/

# Test screenshots from Playwright runs
/*.png
